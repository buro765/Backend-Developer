{
  "name": "java_pricol",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc && cp index.html dist/ && cp style.css dist/",
    "serve": "npx serve dist"
  },
  "private": true,
  "license": "MIT",

  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "serve": "^14.0.0"
  }
}
